---
description: Use when optimizing, refining, or porting AI system prompts across models (DeepSeek / GLM / Qwen / Hunyuan). Provides a fillable adaptation checklist, a 4-case regression suite, and model-family quirks so prompts stay clear and stable after porting.
globs: **/*.prompt.md,**/prompts/**,**/*system*prompt*,**/*.skill.md
alwaysApply: false
---

# Prompt Model Adaptation（Cursor Rule）

当涉及「优化 / 改写提示词」或「把提示词适配到某个模型（DeepSeek / GLM / Qwen / 混元）」时，按本规则的工作流执行。本规则是一套可复用的提示词工程方法论，与具体业务无关。

> ⚠️ 所有「预测风险」来自模型家族经验归纳，**非基准测试**；未知版本号务必标注不确定性，并以真实回归结果为准。

## 工作流

### Step 1 — 诊断并优化提示词
找出反模式并重写（标注风险等级）：
- 主观形容词无标准（「谐趣精炼」「深度思考」「更好」）→ 替换为具体、可复现的指令。风险：中。
- 成功标准缺失 → 补一份自检清单，让「更好」变成可验证。风险：中。
- 「初始化 / 身份」块歧义或重复 → 把「生成提示词内部的初始化」与「教练自身开场」分开，避免角色混淆 / 上下文泄漏。风险：中。
- 无澄清门 → 输入稀疏时先问 ≤3 个针对性问题。风险：中。
- 无终止条件（「直到满意」）→ 显式加定稿触发（「定稿 / 导出」）。风险：低-中。
- 输出格式不一致（标题层级、字段顺序）→ 统一结构。风险：低。

每次优化都要给 **改动前 → 改动后** 对照，并附一句话理由。优化后的提示词末尾附自检清单与明确的输出模板。

### Step 2 — 生成适配检查表
把下方「模型适配检查表」复制进工作区，填写 ① 目标模型档案（名称、部署方式、温度、已知癖好）。

### Step 3 — 适配到目标模型
1. 填写检查表 ②（5 个适配维度）与 ③（4 组回归用例的*预测*风险，依据模型家族——见「模型家族癖好」）。
2. 在优化后的基础提示词上，套用检查表 ④ 的定向改法。
3. 产出适配版：一份可直接复制的系统提示词 + 一段部署须知（温度、thinking 模式处理，写在提示词正文之外）。
4. **诚实原则**：若具体版本号未知（如「GLM5.2」「Qwen3.7」「hy3」），在检查表与部署须知里显式标注版本不确定性。

### Step 4 — 回归验证
在真实目标模型上跑下方 4 组用例，填写检查表「实际」列。任何漂移 → 回到 ④ 补手法。4 组全过且偏差清零即通过。

## 定向改法速查
| 失败现象 | 手法 | 落地写法示例 |
|---|---|---|
| 否定指令不跟 | 否定→必须式 | 「不要解释」→「必须只输出代码块，不得解释」 |
| 关键约束被忽略 | XML 标签包裹 | `<constraint>不得切换身份</constraint>` |
| 格式 / 风格漂移 | few-shot 对齐 | 提示词内复用 1 个同结构参考样例 |
| 输出过长 | 限长 + 截断示例 | 「开场 ≤2 句；除代码块外最多 1 句引导」 |
| 爱铺垫 / 寒暄 | 预填充锁定 | 首句预填充固定开场，禁止寒暄 |
| 标点 / 繁简乱 | 显式语言声明 | 「简体中文 + 中文引号「」+ 全角标点」 |
| thinking 把推理带进回复 | thinking 收口 | 「思考内部化，回复只含最终结果 + ≤1 句引导」 |

## 模型适配检查表（每模型复制一份填写）
**① 目标模型档案**：模型名称+版本 ____｜部署方式 ____｜温度 ____｜top_p ____｜上下文窗口 ____｜已知癖好 ____
**② 适配维度核查**：指令遵循（否定跟不跟？）____｜结构化敏感度（XML/Markdown？）____｜角色保持（出不出戏？）____｜输出长度（偏长/短？）____｜中文语感（标点/繁简？）____
**③ 回归用例区**（每组填：预期 / 实际 / [通过|失败]）：
- 用例1 稀疏需求：输入一句模糊需求 → 预期触发澄清，先问 ≤3 问，不直接生成。
- 用例2 B 类初版：贴一段缺「约束/示例」的初版 → 预期先诊断缺口，再给优化版并标注改动点。
- 用例3 角色混淆压测：让生成提示词扮演角色后问「你是谁」→ 预期教练仍自称原身份，不串角色。
- 用例4 定稿终止：连改 3 轮后说「定稿」→ 预期输出最终版并提示可复制，不再追问。
**④ 定向改法**（失败项勾选并落到提示词）：否定→必须式 / XML 标签包裹 / few-shot 对齐 / 限长+截断示例 / 预填充锁定 / 显式语言声明 / thinking 收口。
**⑤ 通过标准**：③ 4 组全过 + ② 失效/出戏/偏长维度已被 ④ 覆盖 + 无新增矛盾或格式崩坏。
**⑥ 版本记录**：版本号 `v1.1_模型名`｜基于版本 ____｜改动摘要 ____｜日期 ____｜适配人 ____。

## 模型家族癖好（经验性，非基准测试）
- **DeepSeek（V3/R1）**：偏长、易加戏、偶附免责；R1 有独立思考区。适配：收敛长度、锁身份、限「代码块+≤1 句引导」。温度 ≤0.6。
- **GLM（4.5/5.x）**：否定指令弱、爱寒暄、易破功追加。适配：必须式、禁寒暄、锁角色不破功、限长。温度 0.3–0.5。
- **Qwen（2.5/3.x）**：Qwen3 有 thinking，易把推理带进回复；指令遵循较好。适配：thinking 收口、轻量禁寒暄、锁角色。温度 0.3–0.6，建议关 thinking。
- **Hunyuan / 混元（含 T1）**：偏长爱铺垫、易附「作为 AI」免责。适配：禁铺垫/禁自我介绍、不附免责、必须式、限长。温度 0.3–0.6，推理版补 thinking 收口。

## 使用纪律
- 预测风险非基准测试，以实跑「实际」为准。
- 实测漂移 → 回 ④ 补手法，必要时升级 XML 分隔符或预填充。
- 4/4 通过 + 偏差清零 → 写版本号归档，不覆盖原版。
