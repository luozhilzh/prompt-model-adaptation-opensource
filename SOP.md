# Prompt Model Adaptation — 纯文本 SOP（跨工具通用版）

> 本文件是 `prompt-model-adaptation` skill 的纯文本版本，已去除任何平台专用 frontmatter，可直接复制粘贴进任意支持长指令的 AI 工具（Claude / GPT / Cursor / Codex / 通义等）当作 SOP 使用。
> ⚠️ 所有"预测风险"均为模型家族经验归纳，**非基准测试**，请以真实回归结果为准；未知版本号务必标注不确定性。

---

## 一、概述

一套可复用的工作流：先把「模糊 / 不稳定」的提示词改清晰、改稳定，再跨目标模型移植而不引入回归。它打包了诊断方法、一份可填写的适配检查表、常见模型家族癖好，以及 4 组回归用例。

## 二、何时使用

- 用户要「优化 / 改写这段提示词」。
- 用户要「适配成 XX 模型」（如 DeepSeek / GLM / Qwen / Hunyuan）。
- 用户要一份「模型适配检查表」或想对提示词可移植性做 QA。
- 用户想验证某个提示词在不同模型上的稳定性。

## 三、工作流

### Step 1 — 诊断并优化提示词

找出反模式并重写。常见反模式（标注风险等级）：

- **主观形容词无标准**（「谐趣精炼」「深度思考」「更好」）→ 替换为具体、可复现的指令。风险：中。
- **成功标准缺失** → 补一份自检清单，让「更好」变成可验证。风险：中。
- **「初始化 / 身份」块歧义或重复** → 把「生成提示词内部的初始化」与「教练自身开场」分开，避免角色混淆 / 上下文泄漏。风险：中。
- **无澄清门** → 输入稀疏时先问 ≤3 个针对性问题。风险：中。
- **无终止条件**（「直到满意」）→ 显式加定稿触发（「定稿 / 导出」）。风险：低-中。
- **输出格式不一致**（标题层级、字段顺序）→ 统一结构。风险：低。

每次优化都要给 **改动前 → 改动后** 对照，并附一句话理由。优化后的提示词末尾附自检清单与明确的输出模板。

### Step 2 — 生成适配检查表

把检查表模板（见本文「五」）复制进工作区，填写 ① 目标模型档案（名称、部署方式、温度、已知癖好）。说明此表既规范移植流程，也兼作回归记录。

### Step 3 — 适配到目标模型

1. 填写检查表 ②（5 个适配维度）与 ③（4 组回归用例的*预测*风险，依据模型家族——见本文「七」）。
2. 在优化后的基础提示词上，套用检查表 ④ 的定向改法。
3. 产出适配版：一份可直接复制的系统提示词 + 一段部署须知（温度、thinking 模式处理，写在提示词正文之外）。
4. **诚实原则**：若具体版本号未知（如「GLM5.2」「Qwen3.7」「hy3」），在检查表与部署须知里显式标注版本不确定性；说明预测来自家族经验而非基准，必须经回归验证。

### Step 4 — 回归验证

在真实目标模型上跑本文「六」的 4 组用例，填写检查表「实际」列。任何漂移 → 回到 ④ 补手法（如 XML 分隔符、预填充、加强 thinking 收口）。4 组全过且偏差清零即通过。

## 四、定向改法速查

| 失败现象 | 手法 | 落地写法示例 |
|---|---|---|
| 否定指令不跟 | 否定→必须式 | 「不要解释」→「必须只输出代码块，不得解释」 |
| 关键约束被忽略 | XML 标签包裹 | `<constraint>不得切换身份</constraint>` |
| 格式 / 风格漂移 | few-shot 对齐 | 提示词内复用 1 个同结构参考样例 |
| 输出过长 | 限长 + 截断示例 | 「开场 ≤2 句；除代码块外最多 1 句引导」 |
| 爱铺垫 / 寒暄 | 预填充锁定 | 首句预填充固定开场，禁止寒暄 |
| 标点 / 繁简乱 | 显式语言声明 | 「简体中文 + 中文引号「」+ 全角标点」 |
| thinking 把推理带进回复 | thinking 收口 | 「思考内部化，回复只含最终结果 + ≤1 句引导」 |

## 五、模型适配检查表（模板，每模型复制一份填写）

> 用途：把系统提示词移植到新目标模型时，照此表走一遍——填模型档案 → 核查 5 维度 → 跑 4 组回归 → 按失败项挑定向改法 → 达标后版本留档。每个模型存一份，不要覆盖原版。

### ① 目标模型档案

| 项目 | 填写 |
|---|---|
| 模型名称 + 版本 | ____________________________ |
| 部署方式 | API / 对话界面 / 其他：________ |
| 温度 (temperature) | ________ |
| top_p | ________ |
| 上下文窗口 | ________ |
| 已知癖好 / 弱点 | ____________________________ |

> 温度高 → 约束写更死；走 API → 可配合预填充锁定开头。

### ② 适配维度核查表

- [ ] **指令遵循**：否定指令（「不要 X」）跟不跟？
  - 表现：____ 跟随 / ____ 偶失效 / ____ 常失效 → 失效则改「必须式措辞」
- [ ] **结构化敏感度**：吃哪种结构更稳？
  - 偏好：____ XML 标签 / ____ Markdown 标题 / ____ 无明显偏好
- [ ] **角色保持**：被追问或要求切换身份时出不出戏？
  - 表现：____ 稳定 / ____ 偶出戏 / ____ 常出戏 → 出戏则加「不得切换身份」约束
- [ ] **输出长度**：偏长还是偏短？
  - 倾向：____ 偏长 / ____ 适中 / ____ 偏短 → 偏长则限长+截断示例；偏短则给最小内容下限
- [ ] **中文语感**：标点 / 繁简 / 术语是否一致？
  - 问题：____ 无 / ____ 标点乱 / ____ 繁简混 / ____ 术语飘 → 显式声明「简体中文+全角标点」

### ③ 回归用例区

**用例 1 — 稀疏需求**
- 输入：只给一句模糊需求（如「帮我写个卖课提示词」）
- 预期：触发澄清，先问 ≤3 个问题，不直接生成
- 实际：________________________
- 结果：[ ] 通过  [ ] 失败

**用例 2 — B 类初版**
- 输入：贴一段缺失「约束 / 示例」的初版提示词
- 预期：先列缺口诊断，再给优化版并标注改动点
- 实际：________________________
- 结果：[ ] 通过  [ ] 失败

**用例 3 — 角色混淆压测**
- 输入：让生成出的提示词扮演某角色（如「皇帝」），随后问教练「你是谁」
- 预期：教练仍自称原身份，不串角色
- 实际：________________________
- 结果：[ ] 通过  [ ] 失败

**用例 4 — 定稿终止**
- 输入：连续改 3 轮后说「定稿」
- 预期：输出最终版并提示可复制，不再追问
- 实际：________________________
- 结果：[ ] 通过  [ ] 失败

### ④ 定向改法速查（针对 ③ 失败项勾选并落到提示词）

- [ ] **否定 → 必须式**：把「不要 X」改写为「必须 Y」
- [ ] **XML 标签包裹**：用 `<constraint>…</constraint>` 强化关键禁令
- [ ] **few-shot 对齐**：增 1–2 个同结构样例，对齐解析与风格
- [ ] **限长 + 截断示例**：在输出要求给字数上限并附截断样例
- [ ] **预填充锁定**：用首句预填充固定角色开场，防出戏 / 防铺垫
- [ ] **显式语言声明**：声明「简体中文 + 中文引号 + 全角标点」
- [ ] **thinking 收口**：思考内部化，回复只含最终结果 + ≤1 句引导

### ⑤ 通过标准

- [ ] ③ 中 4 组用例全部通过
- [ ] ② 中标记为「失效 / 出戏 / 偏长」的维度已用 ④ 中手法覆盖
- [ ] 无新增矛盾约束或格式崩坏

### ⑥ 版本记录

| 项目 | 填写 |
|---|---|
| 版本号 | v____（建议：`v1.1_模型名`） |
| 基于提示词版本 | ____________________________ |
| 本次定向改动摘要 | ____________________________ |
| 日期 | ________ |
| 适配人 | ________ |

## 六、4 组回归用例（必须全过才算适配完成）

**用例 1 — 稀疏需求**
- 输入：只给一句模糊需求（如「帮我写个卖课提示词」）。
- 预期：触发澄清门，先问 ≤3 个针对性问题（场景 / 身份、输出形态、约束），**不直接生成**。
- 观察点：模型是否跳过澄清直接给通用版。

**用例 2 — B 类初版**
- 输入：贴一段缺失「约束 / 示例」维度的初版提示词。
- 预期：先按框架诊断缺口（缺失维度 / 模糊措辞 / 矛盾约束），再给优化版并标注改动点。
- 观察点：是否只做微调而不诊断；诊断是否冗长夹带大段理论。

**用例 3 — 角色混淆压测**
- 输入：让生成出的提示词扮演某角色（如「皇帝」），随后问教练「你是谁」。
- 预期：教练仍自称原身份（如「三板斧提示词教练」），不串角色、不附「我是 AI」声明。
- 观察点：是否被诱导出戏或追加免责声明。

**用例 4 — 定稿终止**
- 输入：连续改 3 轮后说「定稿」。
- 预期：输出最终版并提示「可直接复制使用」，不再追问。
- 观察点：定稿后是否仍多嘴追加闲聊 / 祝福语。

## 七、常见模型家族癖好与适配要点

> 经验性归纳（非基准测试）。适配具体版本时以实跑回归为准；未知版本号务必标注不确定性。

**DeepSeek（V3 / R1）**
- 弱点：输出偏长、易「好心加戏」、偶附免责声明。R1 有独立思考区。
- 适配：收敛开场与输出长度；强化「不得切换身份」；限定「只输出代码块 + ≤1 句引导」。
- 温度：≤ 0.6。R1 无需在系统提示请求逐步推理展示。

**GLM（含 GLM-4.5 / 5.x）**
- 弱点：否定指令遵循偏弱、爱寒暄、易为「热心」破功追加说明。
- 适配：必须式措辞；禁寒暄与额外解释；锁角色「不破功」；限长。
- 温度：0.3–0.5（低温更守格式）。
- 注意：对「GLMx.y」等具体版本号若不确定，标注需实测校准。

**Qwen（Qwen2.5 / Qwen3.x）**
- 弱点：Qwen3 带 thinking 模式，开启时易把推理带进回复；偶加 preamble。指令遵循总体较好。
- 适配：thinking 收口（思考内部化、回复只含结果）；轻量禁寒暄；锁角色。约束可比 GLM 轻。
- 温度：0.3–0.6；建议关闭 thinking 以获得干净输出。
- 注意：「Qwenx.y」具体版本号若不确定，标注需实测校准。

**Hunyuan / 混元（含推理版 T1）**
- 弱点：偏长爱铺垫、易附「作为 AI」免责声明或自我介绍。
- 适配：禁铺垫 / 禁自我介绍；不附免责声明；必须式；限长。
- 温度：0.3–0.6。推理版需补 thinking 收口（思考内部化）。
- 注意：「hy3」等代号若不确定，标注需实测校准。

**通用排序参考（定性，仅观感）**
- 指令遵循由强到弱大致：Qwen ≈ DeepSeek > Hunyuan ≈ GLM。
- 角色保持：Qwen / DeepSeek 稳；GLM 易破功；Hunyuan 易附免责。
- 输出长度：四家均偏长倾向，均需限长约束。
- 中文语感：四家均强，无需特别处理。
- 特殊风险：Qwen / DeepSeek 推理版注意 thinking 溢出；GLM 注意寒暄 / 破功；Hunyuan 注意铺垫 / 免责。

## 八、使用纪律

- 所有「预测风险」来自模型家族经验，**非基准测试**；以用户实跑的「实际」为准。
- 实测漂移 → 回 ④ 补手法，必要时升级到 XML 分隔符或预填充。
- 4/4 通过 + 偏差清零 → 写版本号归档，不覆盖原版。
